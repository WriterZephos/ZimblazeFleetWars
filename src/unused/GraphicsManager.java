package unused;
/*package graphics;

import java.awt.Graphics;
import java.awt.image.BufferStrategy;

import framework.GameManager;
import framework.Settings;
import launch.ZimblazeFleetWars;

public class GraphicsManager {
	
	private static Graphics g;
	
	static {
		ZimblazeFleetWars.getGameWindow().createBufferStrategy(GameManager.getMySettings().getBuffering());
		g = ZimblazeFleetWars.getGameWindow().getBufferStrategy().getDrawGraphics();
		
	}

	public static void render(){
	
		if (ZimblazeFleetWars.getGameWindow().getBufferStrategy() == null){
		
			ZimblazeFleetWars.getGameWindow().createBufferStrategy(GameManager.getMySettings().getBuffering());
	
			g = ZimblazeFleetWars.getGameWindow().getBufferStrategy().getDrawGraphics();
		
		}
		
		
		
	}

	public static Graphics getGraphics(){
		
		if (ZimblazeFleetWars.getGameWindow().getBufferStrategy() == null){
			
			ZimblazeFleetWars.getGameWindow().createBufferStrategy(GameManager.getMySettings().getBuffering());
		
			g = ZimblazeFleetWars.getGameWindow().getBufferStrategy().getDrawGraphics();
			
		}
		return g;
	}
	
	public static void resetBuffer(int buffering){
		
		ZimblazeFleetWars.getGameWindow().createBufferStrategy(buffering);
		g = ZimblazeFleetWars.getGameWindow().getBufferStrategy().getDrawGraphics();
		
	}	
}
*/
